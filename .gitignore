# --- Gradle ----------------------------------------------------------------
.gradle/
build/
**/build/

# Loom / Fabric Loom run dirs and caches
.loom-cache/
**/.loom-cache/
run/
**/run/

# --- IDEs ------------------------------------------------------------------
.idea/
*.iml
*.ipr
*.iws
.vscode/
*.code-workspace
.fleet/

# Eclipse
.classpath
.project
.settings/
bin/

# --- Java ------------------------------------------------------------------
*.class
hs_err_pid*
replay_pid*

# Built jars (gradle puts them under build/, already covered, but
# catch any stray ones in the working tree).
*.jar
# ...except the Gradle wrapper, which has to be checked in.
!gradle/wrapper/gradle-wrapper.jar
!**/gradle/wrapper/gradle-wrapper.jar

# --- Node / TypeScript (mcp-server) ----------------------------------------
node_modules/
**/node_modules/
dist/
**/dist/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- MCPB bundle (generated) -----------------------------------------------
# Only manifest.json + package.json under mcp-server/mcpb/ are source;
# server/, icon.png, package-lock.json, and the packed .mcpb are built output.
mcp-server/mcpb/server/
mcp-server/mcpb/icon.png
mcp-server/mcpb/package-lock.json
mcp-server/*.mcpb

# --- OS junk ---------------------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Local environment / secrets -------------------------------------------
.env
.env.*
*.local
*.pem
*.key

# Claude Code local config (launch profiles, settings)
.claude/

# --- Logs ------------------------------------------------------------------
*.log
logs/

# MCP server build artifacts
mcp-server/mcpb/
mcp-server/*.mcpb
